[project]
name = "beacon_machine"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
beacon_machine = "beacon_machine.cli:main"

[build-system]
requires = ["hatchling>=1.24.2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/beacon_machine"]
